# Toast Alert Monitor v0.14.5

Мониторинг уведомлений **любых приложений Windows** через центр уведомлений, фильтрация сообщений, голосовое оповещение (TTS), ретрансляция в MQTT (Meshtastic) и **дедупликация** сообщений.

## Возможности

- Прослушивание Toast-уведомлений **любых приложений** (Telegram, Discord, браузеры и др.) – настраивается белый список `AllowedApps`
- Отдельная утилита **LearningMode** (F4) для автоматического сбора имён активных приложений
- Фильтрация по отправителям и ключевым словам (три списка `A/B/C`, логика И/ИЛИ)
- **Дедупликация** сообщений (хэш + расстояние Левенштейна) с настраиваемым окном и порогом
- Озвучивание сообщений через синтезатор речи Windows (TTS) с поддержкой аббревиатур
- **Выбор TTS голоса** из установленных в системе (F2), запоминание в `config.json`
- **Ручной ввод сообщения** (F12) – отправка произвольного текста в TTS и MQTT (минуя фильтры)
- **Тестовое меню** (F3): проверка голоса, MQTT, имитация уведомления
- Отправка отфильтрованных сообщений в MQTT-брокер (JSON для Meshtastic)
- Автоматическое переподключение к MQTT при обрыве связи
- Удаление обработанных уведомлений (immediate/on_exit/never)
- Защита конфигурационного файла от повреждения (атомарная запись, бэкап, устойчивость к синтаксическим ошибкам)
- Восстановление доступа к уведомлениям после сна/пробуждения системы
- Опциональный запрет перехода компьютера в сон
- **Автоматический запуск в Windows Terminal** (если установлен) – цветные эмодзи, корректная работа мыши (настраивается)

## Требования

- Windows 10/11 (версия 19041 или выше)
- Установленный **любой** источник Toast-уведомлений (Telegram Desktop, Discord, браузеры и т.д.)
- .NET 8.0 Runtime (для самодостаточной сборки – не требуется)
- Для наилучшего отображения рекомендуется установить [Windows Terminal](https://aka.ms/terminal) (бесплатно)

## Установка и запуск

1. Скачайте и распакуйте архив с программой (или скачайте один EXE‑файл)
2. Запустите `ToastAlert.exe`
3. При первом запуске программа запросит доступ к уведомлениям Windows – разрешите
4. При необходимости отредактируйте `config.json` (создастся автоматически)
5. Горячая клавиша `R` позволяет перезагрузить конфиг без остановки

## Конфигурация (config.json)

Все настройки хранятся в файле `config.json`. При изменении настроек **не перезаписывайте файл вручную во время работы программы** – используйте горячую клавишу `R` для перезагрузки.

### Секция `Мониторинг`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `CheckIntervalSeconds` | int | 2 | Интервал проверки (сек) |
| `MaxNotificationsPerCheck` | int | 50 | Максимум уведомлений за один цикл |
| `StartMinimized` | bool | true | Запуск в свёрнутом окне |
| `ShowPopupNotifications` | bool | true | Показывать всплывающие уведомления в консоли |
| `PopupTimeoutSeconds` | int | 5 | Время отображения всплывашки (не используется) |
| `LogToFile` | bool | true | Вести лог-файл |
| `LogFilePath` | string | `"telegram_alerts.log"` | Путь к лог-файлу |
| `SaveProcessedIds` | bool | true | Сохранять ID обработанных уведомлений |
| `ProcessedIdsFile` | string | `"processed.json"` | Файл для хранения ID |
| `MaxProcessedIdsCount` | int | 1000 | Максимум хранимых ID |
| `ConsoleOutputEnabled` | bool | true | Вывод информации в консоль |
| `ConsoleMaxMessageLength` | int | 0 | Ограничение длины выводимого сообщения (0 = без ограничений) |
| `ConsoleBeepOnMessage` | bool | true | Звуковой сигнал при новом сообщении |
| `AllowedApps` | массив строк | `["Telegram"]` | Белый список приложений, чьи уведомления обрабатывать. Если пусто – все приложения. |

### Секция `Фильтрация`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `AllowedSenders` | массив строк | [] | Разрешённые отправители (названия каналов/чатов) |
| `Keywords` | массив строк | [] | Основные ключевые слова (список A) |
| `СписокБ` | массив строк | [] | Географические/вторичные ключевые слова (список B) |
| `СписокС` | массив строк | [] | **Приоритетные слова** – при наличии сообщение всегда проходит фильтр |
| `Оператор` | string | `"И"` | Логика между списками A и B: `"И"` или `"ИЛИ"` |
| `BlacklistedSenders` | массив строк | [] | Чёрный список отправителей |
| `BlacklistedKeywords` | массив строк | [] | (не используется) |
| `CaseSensitive` | bool | false | Чувствительность к регистру |
| `PartialMatchSenders` | bool | true | Частичное совпадение имени отправителя (contains) |
| `PartialMatchKeywords` | bool | true | Частичное совпадение ключевых слов (contains) |

### Секция `Дедупликация`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `Enabled` | bool | true | Включить дедупликацию |
| `TimeWindowSeconds` | int | 120 | Время хранения сообщений в буфере (сек) |
| `SimilarityThresholdHigh` | double | 0.95 | Порог, при котором сообщение считается дублем (95%) |
| `SimilarityThresholdLow` | double | 0.85 | Порог для предупреждения (не блокирует) |
| `BufferMaxSize` | int | 50 | Максимум сообщений в буфере |
| `NormalizeTime` | bool | true | Заменять временные метки на `<TIME>` при сравнении |
| `UseLevenshtein` | bool | true | Использовать расстояние Левенштейна (иначе только хэш) |
| `LogDuplicates` | bool | true | Выводить информацию о дублях в консоль |

### Секция `ГолосовоеОповещение`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `Enabled` | bool | true | Включить TTS |
| `TtsEngine` | string | `"windows"` | Движок TTS (пока только Windows) |
| `TtsRate` | int | 1 | Скорость речи (-10..10) |
| `TtsVolume` | int | 30 | Громкость (0‑100) |
| `MaxMessageLength` | int | 300 | Максимальная длина произносимого текста |
| `SpeakOnlyTitle` | bool | false | Озвучивать только заголовок (не используется) |
| `SpeakSenderName` | bool | true | Произносить имя отправителя |
| `SpeakFullMessage` | bool | true | Озвучивать полный текст сообщения |
| `SpeakOnlyOnKeywords` | bool | true | Озвучивать только сообщения, прошедшие фильтр ключевых слов |
| `SpeakTime` | bool | false | Добавлять текущее время в речь |
| `TimeFormat` | string | `"hours_minutes"` | Формат времени: `hours_minutes`, `minutes_only`, `none` |
| `PrefixText` | string | `""` | Текст перед сообщением |
| `SuffixText` | string | `"От канала"` | Текст после сообщения |
| `FallbackBeepOnError` | bool | true | Писк, если TTS недоступен |
| `BeepFrequency` | int | 1500 | Частота писка (Гц) |
| `BeepDurationMs` | int | 200 | Длительность писка (мс) |
| `AbortPreviousSpeech` | bool | true | Прерывать предыдущую речь |
| `MinDelayBetweenMessagesMs` | int | 500 | Минимальная задержка между сообщениями (мс) |
| `VoiceName` | string | `null` | Имя выбранного голоса (сохраняется при выборе через F2) |

### Секция `УдалениеУведомлений`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `DeleteMode` | string | `"immediate"` | Режим удаления: `never`, `immediate`, `on_exit` |
| `DeleteOnlyProcessed` | bool | true | Удалять только обработанные |
| `DeleteAfterSeconds` | int | 0 | Отложенное удаление (не используется) |
| `KeepUnread` | bool | false | Не удалять непрочитанные |

### Секция `ОчисткаТекста`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `RemoveAds` | bool | true | Удалять рекламные фразы |
| `CustomAdsToRemove` | массив строк | [список] | Список фраз для удаления |
| `RemoveHtmlTags` | bool | true | Удалять HTML-теги |
| `RemoveUrls` | bool | true | Удалять ссылки |
| `RemoveEmojis` | bool | false | Удалять эмодзи |
| `RemoveExtraSpaces` | bool | true | Сжимать множественные пробелы |
| `TrimMessage` | bool | true | Обрезать пробелы в начале и конце |
| `MaxMessageLength` | int | 300 | Максимальная длина сообщения после очистки |

### Секция `Аббревиатуры`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `Enabled` | bool | true | Заменять аббревиатуры для TTS |
| `CustomAbbreviations` | объект | {...} | Словарь: ключ – аббревиатура, значение – расшифровка |

### Секция `Логирование`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `Enabled` | bool | true | Включить логирование |
| `Separator` | string | `"---..."` | Разделитель между записями |
| `IncludeTimestamp` | bool | true | Добавлять время |
| `TimestampFormat` | string | `"yyyy-MM-dd HH:mm:ss"` | Формат времени |
| `IncludeSender` | bool | true | Добавлять отправителя |
| `IncludeMessage` | bool | true | Добавлять текст сообщения |
| `IncludeStats` | bool | true | Периодически писать статистику |
| `StatsLogIntervalMinutes` | int | 10 | Интервал вывода статистики (минут) |
| `MaxLogFileSizeMB` | int | 10 | Максимальный размер лог-файла (МБ), после чего создаётся `.bak` |
| `RotateLogs` | bool | true | Ротация логов |
| `LogToConsole` | bool | true | Выводить статистику в консоль |
| `LogToFile` | bool | true | Писать в файл |

### Секция `Звуки`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `EnableSounds` | bool | true | Включить звуковые сигналы |
| `OnNewMessageSound` | string | `"beep"` | Звук при новом сообщении |
| `OnKeywordDetectedSound` | string | `"beep_high"` | Звук при обнаружении ключевого слова |
| `OnErrorSound` | string | `"beep_low"` | Звук при ошибке |
| `CustomSoundWavPath` | string | `""` | Пока не поддерживается |

### Секция `Дополнительно`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `EnableHotkeys` | bool | true | Включить горячие клавиши |
| `HotkeyStats` | string | `"S"` | Клавиша для статистики |
| `HotkeyConfig` | string | `"C"` | Клавиша для конфигурации |
| `HotkeyMute` | string | `"M"` | Клавиша отключения звука |
| `HotkeyExit` | string | `"Escape"` | Клавиша выхода |
| `AutoStartWithWindows` | bool | false | Добавить в автозагрузку (не реализовано) |
| `RunInBackground` | bool | true | Скрывать окно (не реализовано) |
| `ShowTrayIcon` | bool | false | Иконка в трее (не реализовано) |
| `Language` | string | `"ru"` | Язык интерфейса (не используется) |
| `DebugMode` | bool | false | Детальные сообщения об ошибках |
| `DeveloperMode` | bool | false | Режим разработчика (не используется) |
| `RememberVolume` | bool | true | Сохранять изменённую громкость TTS |
| `MqttEnabled` | bool | true | Глобальное включение MQTT (наряду с `MQTT.Enabled`) |
| `PreventSleep` | bool | false | Запретить переход компьютера в сон |
| `UseWindowsTerminal` | bool | true | Автоматический запуск в Windows Terminal |

### Секция `Приоритеты`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `PriorityKeywords` | массив строк | `["ракетная","Валуйки"]` | Слова, при которых сообщение прерывает речь |
| `InterruptCurrentSpeech` | bool | true | Прерывать текущую речь |
| `HighPriorityBeep` | bool | true | Дополнительный сигнал |

### Секция `MQTT`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `Enabled` | bool | false | Включить отправку в MQTT |
| `TestMode` | bool | false | Тестовый режим (вывод в консоль без реальной отправки) |
| `Broker` | string | `"127.0.0.1"` | Адрес MQTT-брокера |
| `Port` | int | 1883 | Порт брокера |
| `Topic` | string | `"telegram/alerts"` | Топик для публикации |
| `ClientId` | string | `"telegram_alert_..."` | Идентификатор клиента (уникальный для каждого экземпляра) |
| `FromNodeId` | uint | 1234567890 | ID узла-отправителя (Meshtastic) |
| `ToNodeId` | uint | 4294967295 | ID получателя (широковещательная рассылка) |
| `ChannelIndex` | int | 0 | Индекс канала Meshtastic |
| `IncludeTimestamp` | bool | true | Добавлять временную метку |
| `TimestampFormat` | string | `"HH:mm:ss"` | Формат времени |
| `MaxPayloadBytes` | int | 200 | Максимальный размер сообщения в байтах |
| `ReconnectEnabled` | bool | true | Автоматически переподключаться |

### Секция `ПроверкаСвязи`

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `ИнтервалСек` | int | 30 | Интервал между попытками переподключения MQTT (сек) |
| `МаксПопыток` | int | 0 | Максимум попыток (0 = бесконечно) |
| `ТаймаутМс` | int | 3000 | Таймаут ping-проверки (мс) |
| `МаксВремяПопытокСек` | int | 0 | Максимальное время всех попыток (0 = бесконечно) |

## Горячие клавиши (по умолчанию)

| Клавиша | Действие |
|---------|----------|
| `S` | Показать статистику |
| `C` | Показать **полную конфигурацию** (все параметры) |
| `M` | Вкл/Выкл звук TTS |
| `+` / `-` | Увеличить/уменьшить громкость TTS на 10% |
| `R` | Перезагрузить `config.json` |
| `Q` | Вкл/Выкл MQTT (динамически) |
| `D` | Очистить буфер дедупликации |
| `H` / `F1` | Показать справку |
| `F2` | Список доступных голосов TTS / переключить голос (выбор запоминается) |
| `F3` | Тестовое меню (голос, MQTT, имитация уведомления) |
| `F4` | Запустить **LearningMode** (сбор приложений) |
| `F12` | Ручной ввод сообщения (озвучить + отправить в MQTT, минуя фильтры) |
| `Esc` / `Ctrl+C` | Остановить программу (мгновенное завершение) |

## Дополнительные утилиты

### LearningMode (сбор приложений)

Запускается по `F4` или из командной строки (`ToastAlert.LearningMode.exe`).  
Собирает имена приложений, от которых поступают уведомления, и сохраняет их в `discovered_apps.txt`.  
Пользователь может скопировать нужные имена в секцию `AllowedApps` конфига.

## Рекомендации

- Для наилучшего отображения установите **Windows Terminal** из Microsoft Store – программа автоматически запустится в нём (если параметр `UseWindowsTerminal` не отключён). В нём эмодзи цветные, правый клик не мешает, прокрутка работает.
- Если Windows Terminal не установлен, программа работает в обычной консоли, где эмодзи могут быть чёрно-белыми.
- При редактировании `config.json` вручную соблюдайте синтаксис JSON (запятые, кавычки). При ошибке программа выдаст понятное сообщение и не запустится, пока вы не исправите.

## Логирование и файлы

- `telegram_alerts.log` – основной лог (с ротацией по размеру)
- `processed.json` – ID обработанных уведомлений (чтобы не дублировать)
- `config.json.bak` – резервная копия конфигурации при успешной записи
- `discovered_apps.txt` – создаётся LearningMode

## Сборка из исходников

```bash
git clone <repo>
cd ToastAlert
dotnet restore
dotnet build -c Release
Публикация самодостаточного EXE
bash
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o publish
Лицензия
MIT © 2026 Quantum Croak Industries

*Последнее обновление: 2026-05-26*